# Changelog

## [Unreleased]

## [0.17.0] - 2026-08-06

### Added

- 增加医疗、法律和财务信息整理契约与新手模板，分别要求症状与红旗信号、司法辖区与时效、目标期限与流动性等关键事实，并为结构化 Decision Brief 和 Markdown/HTML 导出提供对应字段。
- macOS 原生外壳增加 App Store 分发身份、隐私清单、子进程权限与沙盒兼容服务控制，为后续商店提交流程保留独立构建路径。
- 增加对话框可访问性、平板首页、CC Switch 历史连接、准备度失败和专业领域完整流程的端到端回归覆盖。

### Changed

- 服务端会把已识别的医疗、法律和财务问题、模板与契约强制路由到高风险控制，并执行对应的关键事实和人工门禁；专业输出同时带有用途限制 Prompt 与固定免责声明，不应被用作诊断、法律意见或投资建议。
- 桌面工作区提升首次使用引导、专业模板入口、结果说明、设置反馈和响应式布局；关键阻断、未验证事实与下一步操作使用更直接的语言。
- GitHub 中英文首页改为产品价值和双平台下载优先，以真实界面说明公开审议、人类确认、本地数据边界和专业场景限制。
- 更新检查会识别 App Store 分发；GitHub 直发版只检查版本并打开官方 Release，在补齐可独立验证的发布者签名前不会在应用内自动下载或执行更新包。

### Fixed

- 修复打包启动器重建运行身份后丢失明确网页 Build ID 绑定的问题，避免错误复用同版本但不同构建的前端服务。
- 强化原生壳的启动、重连和退出管理，避免并发启动、旧服务占用或窗口恢复导致工作区停在不可操作状态。

### Testing

- 通过完整后端、前端单元、Chromium 端到端、TypeScript、Next.js 生产构建、发布一致性和桌面打包边界检查。

## [0.16.0] - 2026-08-05

### Added

- Decision Analysis is now the sole public Council Lab product boundary, with release checks that reject out-of-scope feature paths and content.
- The decision workspace gets a stable desktop shell, a resizable non-compressing sidebar, mobile focus recovery, and a tighter phone composer layout.

### Changed

- README and release messaging now describe the decision-analysis workflow, local data boundary, real-provider setup, and the 50-request acceptance evidence without presenting the product as an autonomous decision maker.

### Testing

- Full isolated Chromium regression: 53/53 passed with the backend on a dedicated loopback port.

## [0.15.7] - 2026-08-05

### Added

- macOS 桌面版增加 SwiftUI 原生外壳，提供系统级侧栏、工具栏、前进后退、快捷键、启动状态和失败恢复，同时继续复用现有网页工作台与本地审议引擎。
- GitHub 中英文首页改为产品价值优先的清晰结构，补充三步上手、真实 API 验收、数据边界和原生桌面架构说明。
- 公开一份不含模型正文、凭据或 CC Switch 内部路由标识的 50 次真实请求验收摘要。

### Changed

- 网页侧边栏可以在 190–360px 之间拖动或使用键盘调整，也可以收起为稳定的 76px 图标栏；宽度与收起状态会保存在当前浏览器。
- macOS 与 Windows 启动器会核对前端 Build ID，发现仍在运行的是旧网页构建时自动重启前端，避免安装更新后继续显示旧界面。
- 最终综合的“关键理由”改为从本题公开答案与席位发言中提取，DecisionBrief 会显示这些题目相关理由，不再写入固定流程占位语。

### Fixed

- 边界说明不再被误收为关键理由；长篇反对意见在最终结果中压缩为核心一句，同时在公开讨论和少数意见中保留完整原文。
- 移除侧栏底部多余的“工作区”标签，并保持手机抽屉使用完整导航，不受桌面收起状态影响。
- 原生 macOS 外壳在复用本机服务前同时核对网页构建、安装路径与版本、内部令牌标识，避免误连同版本的另一份 Council 安装。

### Testing

- 使用 CC Switch 与 `gpt-5.6-sol` 完成 10 个真实 Council Run、共 50 次模型请求：50/50 成功，0 次失败、重试或降级；修复后另用 1 个完整 Run、5 次真实请求回归题目相关理由和分歧摘要。
- 通过 405 项后端测试（84.99% 覆盖率）、37 项前端单元测试、4 项 Swift 身份回归、TypeScript、Next.js 生产构建、Swift Release 构建、启动脚本语法和发布一致性检查。

## [0.15.5] - 2026-08-03

### Changed

- 历史运行列表支持服务端分页和跨页搜索，长期使用时仍能保持较快打开速度。
- 混合中英文文本使用更保守的 token 估算，Chat Completions 不再发送不受支持的 reasoning effort。
- 长时间 SSE 连接发送命名 `ping` 心跳，前端会忽略心跳并保持实时讨论连接。
- CCSwitch 检测结果独立报告路由可用性，Provider 删除、准备度校验和运行分页 API 的契约更明确。

### Fixed

- Provider HTTP 403/429/5xx 和外层超时现在会保留安全、可操作的错误信息及状态标签，不泄漏上游响应正文。
- SQLite 运行列表新增迁移索引；读取旧数据库时仍能安全升级。

### Testing

- 完成 20 次 CCSwitch 真实 AI 圆桌演练，覆盖中英双语、三种模式、两种工作流、三种输出契约、插话、总结、取消、停止/恢复和长上下文；16 次完成、1 次主动取消、3 次因上游拒绝或超时保留可恢复状态。
- 通过 400 项后端测试、37 项前端单元测试、TypeScript 和 Next.js 生产构建，并重建启动 macOS `Council.app`。

## [0.15.4] - 2026-08-04

### Changed

### Fixed

- Gitee Release 大附件改用系统 `curl` 执行 multipart 上传，提供真实的端到端总耗时上限和持续传输进度；访问令牌只经标准输入配置传递，不出现在进程参数、日志或异常文本中。
- Gitee 历史版本补发工作流增加一次性本机 runner 路由，海外 GitHub runner 到 Gitee 出现极低速或连接超时时，可在国内网络完成上传；默认仍使用 GitHub 托管 runner。

## [0.15.2] - 2026-08-03

### Changed

- 将结果页高风险控制面拆为独立展示组件，父页面继续持有 API 调用、授权信息和状态机，降低高风险界面的维护与审查复杂度，不改变用户流程。
- 移除未使用的 React Query 生产依赖，缩小前端依赖面。
- GitHub Tag 发布默认只生成 GitHub Release；只有显式设置仓库变量 `GITEE_RELEASE_ENABLED=true` 时才允许自动同步 Gitee，保留手动补发能力。

### Fixed

- 修复 `make backend-test` 对调用目录和全局 `pytest` 的隐式依赖；命令现在固定使用仓库虚拟环境，并从仓库根目录运行完整后端测试集。
- Provider 启动回退不再假定 `ccswitch` 或 `mock` 键一定存在；缺少内置 Provider、未验证 CC Switch 模型或已有可用自托管 Provider 时均会选择可用配置，找不到任何带默认模型的 Provider 时明确失败。

### Testing

- 通过 386 项后端测试，覆盖率 84.07%；通过 36 项前端单元测试、TypeScript、Next.js 生产构建、npm 生产依赖审计（0 漏洞）、100 项 Chromium E2E，以及 macOS 启动器语法、Info.plist 与代码签名验证。

## [0.15.1] - 2026-08-03

### Fixed

- Windows 发行冒烟测试现在保留前后端 stdout/stderr、进程退出状态和最后一次健康检查错误，后续打包启动失败会直接显示根因，不再只等待超时。

### Testing

- GitHub Actions 完整验证、macOS 自包含打包与启动器/更新器冒烟、Windows 自包含打包与服务启动、残留恢复和更新回滚冒烟全部通过。

## [0.15.0] - 2026-08-03

### Fixed

- 修复真实模型演练中席位显式等待时间未传入 Provider 客户端的问题，并为 Medium、Xhigh、Max 推理档补齐超时降档路径；长响应仍受 Run 总时限约束，已经完成的席位继续保留。
- 限制上游请求 ID 的持久化长度，避免异常 Provider 响应头破坏运行记录。

### Documentation

- 重写中英文 README 首屏，先说明“把复杂问题变成可检查的决策记录”这一核心价值，再按“三步开始 → 工作流 → 使用边界”组织 GitHub 与 Gitee 介绍；补充双站仓库/Release 入口，减少功能堆砌，不新增或夸大质量结论。
## [0.14.0] - 2026-08-02

### Added

- GitHub Release 完成后可使用仓库 Secret `GITEE_ACCESS_TOKEN` 自动创建或更新同版本 Gitee 发行版，上传 macOS、Windows 和 SHA-256 文件并复用 CHANGELOG 更新说明；另提供可补发历史 Tag 的手动工作流。

### Fixed

- Gitee Release 大附件上传使用独立的 30 分钟超时，避免约 70 MB 的 macOS/Windows 自包含安装包在默认 5 分钟写入上限到达后失败；普通 API 请求继续使用较短超时，两个超时均可通过受限环境变量调整。

所有重要变化记录在此文件。版本遵循 Semantic Versioning。

## [0.13.0] - 2026-08-01

### Added

- 高风险控制新增证据来源、版本、来源时间、有效期、内容哈希和关键事实值绑定；文档与工具证据必须提供 SHA-256，修改事实后旧证据自动失效。
- 新增追加式证据记录、追加式核验记录和追加式专业复核记录。SQLite schema 升级到 v12，三类记录均由触发器禁止更新和删除，旧数据库继续无损升级。
- 新增证据提交、授权复核人核验证据、领域专业复核 API；医疗、法律、投资、合规和生产事故使用独立角色白名单和领域匹配门禁。
- 高风险控制面显示证据来源、时间、有效期、冲突状态、专业角色和全部阻断原因；Markdown/HTML 导出同步显示脱敏的证据与专业复核状态。
- 增加 50 个医疗、法律、投资、合规和生产事故控制场景回归，以及手机端“追加证据—独立核验”Playwright 流程。

### Changed

- 仅填写关键事实不再足以形成报告；每项关键事实必须绑定当前有效且经授权人员独立核验的证据。
- 报告形成后必须由匹配领域的专业角色逐域复核，才能创建最终审批；专业复核人与最终审批人必须是不同授权主体。
- 医疗紧急红旗、过期证据、冲突证据、角色错配、专业复核缺失或过期全部失败关闭，不能进入批准或完成状态。
- 风险规则分类器升级到 v2，返回规则置信度和强制人工确认标记；置信度只描述分类规则命中强度，不代表结论正确率。

### Security

- 专业角色属于复核人责任声明，系统验证授权密钥、领域匹配、证据快照和报告哈希，但不验证执照真伪，也不会执行开药、交易、法律提交、合规放行或生产变更。

## [0.12.0] - 2026-08-01

### Added

- 增加可选“先独立初答”工作流：每个席位只读取冻结问题、资料和用户明确选择的记忆，完成后再公开汇总；默认连续审议保持兼容。
- 新增 `workflow_strategy`、Turn 阶段标记和 SQLite schema v11 空迁移；旧 Run 不被原地重写，缺少字段时按连续审议读取。
- Benchmark 增加 `independent_council` 实验策略，报告可比较独立初答与连续审议的质量、成本和延迟。

## [0.11.0] - 2026-08-01

### Added

- 完成 `DecisionBrief v1` 基础能力：新完成的 Run 会生成经严格 schema 与语义规则校验的结构化决策简报，明确区分推进状态、席位支持、未解决问题、假设、行动、重开条件、少数意见和限制。
- SQLite schema 升级到 v6；简报使用独立的追加式 `decision_briefs` 表保存，不回写已完成 Run、Turn、高风险审批或审计记录。新增只读 `GET /api/runs/{run_id}/decision-brief`，并对 Run ID 执行边界校验。
- 结果页和 Markdown/HTML 导出增加结构化简报；原始总结继续完整保留，旧 Run 没有简报时仍使用原有结果视图。
- 增加不可变情景分叉：完成后的 Run 可从讨论前、任一已存在席位后或总结前创建新 Run，并以结构化视图比较父子简报。复用发言会明确标记，用量只统计子 Run 新调用。
- SQLite schema 升级到 v7，新增追加式 `run_forks` 父子关系；分叉创建、子 Run 与关系记录在同一事务提交，创建 API 支持持久幂等。
- 增加人工批准的决策记忆：完成 Run 可生成无模型调用的候选，用户可以编辑后逐条批准或拒绝；批准后的记忆支持停用、重新启用和删除，所有操作保留追加式记录。
- SQLite schema 升级到 v8，新增 `memory_proposals`、`project_memories`、`memory_actions` 和不可变 `run_memory_snapshots`；新 Run 只注入用户明确选择的批准记忆，实际快照与 Run 同事务保存。
- 增加多标签决策准备度：进入完整审议前显示目标、约束、方案、成功标准和高风险关键事实缺口；用户仍可明确覆盖，覆盖原因作为独立记录保存。
- 增加关键主张来源与争议标签；模型给出的 URL 永远从“有引用，未核验”开始，席位反对单独显示，模型共识不会升级为事实验证。
- SQLite schema 升级到 v10；回访改为追加式 `decision_outcomes`，原 Run 和原 Claim 不再因回访而改写，Claim 的结果支持/反驳通过追加式 `claim_outcomes` 计算当前状态。只有用户显式删除整个普通 Run 时，关联主张和回访才会随本地记录一并清除；高风险审计仍不可删除。
- 增加一般决策、产品评审和技术架构评审三类输出契约。契约只改变输入检查、席位/总结指导和 `DecisionBrief v2` 的强类型扩展，不改变固定四席身份、模型调用上限、审批或安全门禁；旧 Run 默认按一般决策读取。

### Changed

- 最终综合先持久化，再生成简报；如果简报校验或写入失败，Run 回到可恢复的确认状态，重试只固化简报，不重复总结席或讨论席模型调用。
- 席位支持只从公开发言中的明确表态推导，不显示百分比置信度，也不把模型共识描述为事实验证；存在明确反对时保留少数意见。
- 高风险分叉始终创建新的控制记录且不继承父 Run 审批；普通 Run 持久化失败时子控制记录进入 `ACTION_BLOCKED`，父审批和审计保持历史状态。
- 记忆候选不代表事实验证；记忆预览明确显示本次实际注入内容，停用、删除或过期的记录会被排除，不会静默注入。
- 旧回访读取保持兼容；新的重复回访请求使用持久幂等键，同一请求不会创建两份结果记录。
- Markdown/HTML 导出现在保留关键主张、依据标签、未核验引用、争议摘要和后续结果依据。
- 新建页可选择输出契约，结果页和 Markdown/HTML 导出展示产品验证或架构迁移/回滚专属字段；Benchmark 结果可按契约分组统计执行指标。

### Fixed

- 一般决策继续使用后端稳定默认契约，避免给旧客户端的创建请求强加新字段；产品评审与技术架构评审仍显式固化所选契约。
- 修复完成页初始化轮询与“沉淀记忆”并发时，迟到的空响应清除刚生成候选的问题。
- 高风险问题可直接进入关键事实、专业升级和人工审批控制流，不再被普通准备度提示挡在 Run 创建之前；普通问题的明确覆盖门禁保持不变。

### Testing

- 增加 DecisionBrief 三种状态、阻塞规则、少数意见、不可变存储、失败重试、SSE 重放、API 输入校验、旧 Run 兼容、迁移升级、导出和移动端结果页测试。
- 增加分叉 checkpoint 边界、模式不兼容、父记录不变、用量归属、API 幂等、v6→v7 升级/失败恢复、高风险失败关闭和“创建分叉—打开子 Run—比较结果”E2E。
- 增加记忆候选生成、编辑批准/拒绝、停用/删除、注入快照原子回滚、v7→v8 升级/失败恢复、API 重复输入校验和“批准记忆—预览—创建新 Run”E2E。
- 增加准备度多标签边界、覆盖记录、模型引用降级、主张争议、追加式回访、结果支持/反驳、导出、显式删除、v8→v9→v10 升级/逐级失败恢复，以及“查看缺口—明确继续”和“回访后主张状态变化”E2E。
- 增加契约注册、默认兼容、非法输入、Prompt 指导、强类型简报扩展、导出、评测分组和“选择产品契约—创建 Run—查看专属结果”E2E。
- 完成 50 项生产构建实机 QA；最终 49 条 Chromium E2E 全通过，并补充 1440×900 桌面与 375×812 手机视觉、宽度和错误终态检查。
- 通过 243 项后端测试（82.08% 覆盖率）、10 项前端单元测试、TypeScript、Next.js 生产构建、发布一致性检查和 npm 依赖审计（0 漏洞）。

## [0.10.1] - 2026-07-31

### Changed

- 历史记录增加真实加载、失败与无匹配状态；记录按每批 30 条渐进显示，避免 100 条以上历史一次创建大量 DOM，搜索和状态筛选仍覆盖全部本地记录。
- Service Worker 注册失败会安全降级；只缓存公开 manifest 与图标、只拦截图标请求，不缓存 API、讨论、页面或凭据。

### Fixed

- 普通审议不再每 2.5 秒探测不存在的高风险控制接口。新 Run 明确保存 `high_risk_control`，旧 Run 仅兼容探测一次，高风险流程仍持续刷新控制面。
- macOS 与 Windows 自包含打包现在会复制 `frontend/public`；修复正式包缺少 `sw.js` 和 PWA 图标而持续产生 404 的问题。
- 修复历史页读取期间短暂显示“还没有审议记录”，以及 100 条以上记录一次性渲染造成的手机端卡顿风险。

### Testing

- 在真实 `Council.app` 和 CC Switch 上完成超过 50 次本机操作，覆盖创建、四席调用、最终补充、第五次总结、沉浸模式、模板、预算、高风险开关、历史筛选、8 个 Provider、席位设置、手机连接与 375×812 窄屏页面；真实 Run 完成 5 次模型调用并正确回答确定性算术。
- 新增普通 Run 零高风险探测、Service Worker 静态资源、105 条历史分批加载、打包公开资源和高风险标记持久化回归测试。
- 通过 207 项后端测试（79.88% 覆盖率）、10 项前端单元测试、42 项 Chromium E2E、TypeScript 检查、Next.js 生产构建与 npm 高危漏洞审计。

## [0.10.0] - 2026-07-31

### Added

- 首页增加明确的“自动总结”开关；默认仍等待用户确认，高风险模式在代码层禁止自动总结。
- 快速档为短定义和确定性算术增加保守的 `1 个讨论席 + 1 个总结席` 路线；决策、风险、预测、模糊数量和高风险领域保持完整 `4+1`。
- 运行页按实际参与席位显示预计调用、已完成席位、真实模型调用数与 Token；历史页补齐 queued、running、awaiting、completed、cancelled、failed 和 stopped 独立状态。

### Changed

- 诘问席必须给出可证伪反例、失败条件或关键假设，并说明什么证据会推翻判断；观澜席必须保留真实的未解决分歧或决策边界。
- 最终答案页面及 Markdown/HTML 导出顶部固定显示“未经过外部事实核验”，不把模型共识描述成事实验证。
- Run 只记录实际参与席位，短任务的 Provider 摘要不再误列未调用的席位；用户保存的五席配置保持不变。

### Fixed

- 修复 `auto_summarize=true` 在进入总结前短暂持久化并广播 `awaiting_final_input`，导致轮询客户端与手工总结发生竞态的问题。
- 修复取消接口返回 `200` 时 Run 仍可能保持 `running` 的问题；成功取消现在等待任务收敛、返回并持久化 `cancelled`，且只记录一次取消事件。
- 修复历史记录未单独映射的状态可能错误显示为“已取消”，以及一席短任务仍出现“四席/第五次调用”文案的问题。

### Testing

- 新增自动总结事件顺序、调用中取消、短任务分类与两次调用、批判提示契约、动态席位 UI、核验警告和状态筛选回归测试。
- 通过 206 项后端测试、10 项前端单元测试、37 项 Chromium 生产构建 E2E、TypeScript 检查和 Next.js 生产构建。
- 使用 CC Switch / `gpt-5.6-sol` 做有界真实回归：定义与算术均 2 次调用，决策与批判均 5 次调用并自动完成，取消路径返回 `cancelled`；该小样本只证明工作流行为，不构成模型质量或稳定时延声明。
- 强化跨平台发布门禁：自包含包必须同时证明无内部令牌访问更新接口返回 `403`，携带启动器令牌后才能读取版本。
- Release notes 生成器现在拒绝空版本段，测试同时保证只包含当前版本正文，不会夹带旧版本内容。

## [0.9.2] - 2026-07-31

### Fixed

- 修复 macOS 与 Windows Release 烟测仍以无认证请求直连受保护 `/api/update/status`、导致 `v0.9.1` 在打包后无法发布附件的问题。
- 两个平台的发布烟测现在读取启动器生成的私有 `backend-access.token`，仅在测试进程内为受保护接口添加内部认证头，不放宽产品请求边界。

### Testing

- 增加 Release 工作流契约回归测试，并通过无标签完整发布预演验证 macOS/Windows 打包、重复启动、部分恢复、更新替换和 artifact 上传。
- `v0.9.1` 工作流在发布附件前失败；其浏览器到 loopback 后端统一请求边界等安全改进与本修复一起由 `v0.9.2` 正式发布，不重写已推送标签。

## [0.9.1] - 2026-07-31

### Added

- 增加浏览器到 loopback 后端的统一请求边界：除 `/api/health` 外，全部现有及未来 `/api/*` 路由都要求启动器生成的服务端内部令牌。
- macOS 与 Windows 的源码和自包含启动器共享用户私有的 `backend-access.token`，并通过不泄露令牌的进程标识识别、替换继承了旧令牌的进程。
- 增加恶意网页、空来源、简单表单、`text/plain`、跨站 Fetch Metadata、异常 Host、端口混淆和 IPv4/IPv6 边界回归测试。

### Changed

- 浏览器 API 客户端固定使用 Next.js 同源代理；不再支持通过公开 `NEXT_PUBLIC_*` 地址从浏览器直连 FastAPI。
- 自定义开发端口必须在后端显式设置 `COUNCIL_FRONTEND_PORT`；默认只信任产品端口 `3000`，手机端局域网访问保持兼容。

### Fixed

- 修复恶意网页可以向 `127.0.0.1:8001` 发送简单跨站请求并触发 Provider 等状态变更的问题。
- 修复内部令牌文件被删除或损坏后，后端与前端可能继承不同令牌并导致界面 API 持续返回 `403` 的问题。

### Security

- Next.js 会删除浏览器伪造的内部认证头，只从服务端环境注入真实令牌；令牌不进入浏览器 JavaScript、URL、SQLite、诊断包或日志。
- FastAPI 使用恒定时间比较校验令牌，并额外拒绝恶意或空 Origin、非同源 Fetch Metadata、非预期前端端口及 DNS rebinding 风格 Host。

## [0.9.0] - 2026-07-30

### Added

- 增加可选高风险决策支持模式，覆盖医疗、法律、投资、合规和生产事故领域的服务端风险分级与关键事实门禁。
- 增加独立持久化状态机、明确拒答/升级状态、人工报告准备和审批流程；审批绑定 Run、动作类型、动作参数哈希、报告哈希、请求者、复核者和有效期。
- 增加高风险审计记录，记录状态转换、拒绝操作、风险覆盖、事实变更、审批申请/决定/撤销/过期/消费和旧路由绕过，并使用 SQLite 触发器拒绝审计更新或删除。
- 首页增加高风险模式开关，讨论页增加适配桌面和手机的高风险控制面板，支持填写关键事实、准备非约束性报告、请求复核和查看审批状态。
- 讨论页增加脱敏审计时间线，只显示控制事件、结果和时间，不向浏览器暴露请求者、复核者、绑定哈希或审批内部元数据。
- Markdown 与 HTML 导出增加高风险公开控制摘要，仅披露控制状态、风险等级、检测领域和关键事实填写进度，并明确报告不代表事实核验、专业参与或监管合规。

### Changed

- Candidate 结构化字段增加来源标记；新席位未执行可靠结构化提取时保存为空并标记为 `none`，不再写入通用理由、假设或风险模板。
- 旧 Run 中没有来源标记的 Candidate 在数据库升级时保留原字段并标记为 `legacy_default`，避免将历史模板误认为模型明确输出。
- SQLite schema 升级到 v5；普通 Run 继续使用原有流程，高风险控制记录使用独立表并在启动恢复时保持阻断，不自动恢复模型调用。

### Security

- 高风险 Run 必须在首次模型调用前建立控制记录；普通总结、重试、续跑、重跑、删除、决策回访和取消入口不能绕过高风险状态机。
- 请求者和复核者实施服务端绑定及职责分离；复核密钥只从服务端环境配置读取，不进入 SQLite、浏览器持久化、错误或审计日志。
- 审批只能消费一次；事实、报告或动作发生变化会原子撤销旧审批，过期、拒绝、撤销和跨 Run 重放均失败关闭。
- P0 只生成非约束性决策支持报告，不执行网络搜索、代码或生产命令，也不执行任何外部副作用。

## [0.8.4] - 2026-07-30

### Added

- 圆桌讨论增加沉浸模式：桌面端可进入浏览器全屏，浏览器拒绝全屏时自动退回应用内沉浸；手机端使用安全区适配的应用内沉浸，不强制调用浏览器全屏。
- 沉浸状态保留议题、公开讨论、最终答案、插话、重试和错误恢复，隐藏侧栏、席位看板、资料条与导出等次要界面，并支持 `Esc`、明确退出按钮和焦点恢复。

### Changed

- macOS 与 Windows 自包含启动器按“安装目录 + 版本”生成运行标识；只有后端和前端都属于当前安装包及当前版本时才复用现有服务。
- 启动器发现旧 Council 源码实例或另一份 Council 安装占用端口时，会核对服务身份并替换旧实例；其他程序占用端口时仍拒绝抢占或结束进程。
- 沉浸正文使用居中的 760px 阅读列和 16px 正文，并补齐窄屏、安全区、无横向溢出及键盘可访问性处理。

### Fixed

- 修复正式版启动时误复用旧源码后端，导致界面提示“有更新”但 `can_auto_update=false`、无法显示“下载并安装”的问题。
- 修复服务身份检查与结束端口进程之间的竞态，只结束健康检查前后均属于同一 Council 服务的 PID，避免端口换手时误伤其他程序。
- 修复仅凭公开健康响应识别进程的问题；启动器现在还会核对可执行文件、工作目录和 Council 文件结构，伪造服务名的其他程序不会被结束。
- 修复同一安装路径原地升级后仍复用旧版本进程的问题，并让 macOS 与 Windows 发布测试覆盖旧版本替换。
- 修复原生全屏退出、全屏申请失败和移动端切换时沉浸状态不同步或焦点丢失的问题。
- 修复用户在全屏申请尚未完成时退出，延迟完成的请求又重新进入沉浸模式的问题；进入沉浸后焦点会落在明确的退出按钮上。

## [0.8.3] - 2026-07-30

### Fixed

- 切换席位 Provider 时同步模型、协议、推理档位和超时时限，避免总结席继续沿用旧的 30 秒限制而无法生成最终答案。
- 旧席位配置和运行快照只迁移一次；用户明确设置的 30 秒保持不变，Provider 后续调整也不会篡改已固化的历史运行。
- 最终综合超时或临时失败后可直接重试，保留四席讨论、清除旧错误，并避免重复调用已经完成的席位。

## [0.8.2] - 2026-07-29

### Fixed

- 将自包含发行包的 Next.js 静态资源复制到 standalone 配置实际使用的 `.next-release/static`，修复首页 HTML 可打开但全部 JavaScript/CSS 返回 404 的问题。
- macOS 和 Windows 发布烟测新增真实页面静态资源请求，避免只检查 HTML 文本而漏过不可交互的空壳页面。

## [0.8.1] - 2026-07-29

### Fixed

- 将源码桌面运行构建、普通验证构建和发行构建分别隔离到独立目录，避免运行中的 Next.js 服务被后续构建覆盖静态资源后出现整页加载失败。
- 增加首席席位超时且尚无公开发言时的端到端回归，确保失败原因和当前席位重试入口仍可正常显示。

## [0.8.0] - 2026-07-29

### Added

- 增加“设置 → 诊断与支持”，可手工导出仅含运行环境、存储完整性和 Provider 就绪摘要的脱敏 ZIP；不包含对话、资料、日志正文、凭据、配对令牌、模型名或本机路径。
- API 错误增加稳定错误码和请求排错编号，响应头与错误正文可关联同一次失败。
- 已知 OpenAI-compatible 模型使用匹配的 `tiktoken` 计数；未知 Provider 使用明确标记的保守 UTF-8 估算，并用 100 条混合语料回归预算和最新用户输入保留。
- Release 产物增加 GitHub build provenance attestation，关联发布包、workflow 与 commit。
- 业务 SQLite 增加显式 schema 迁移、升级前一致性备份、失败自动恢复和最近 5 份备份保留。
- Run 创建、重跑、取消、推进、插话、重试、续跑和总结增加跨进程持久幂等协议，前端网络失败使用同一键重试。
- 前端增加 Vitest API 单元测试与 Axe 可访问性回归；CI 每周在 Chromium、Firefox 和 WebKit 的生产构建上执行 E2E。
- 评测增加加强直接回答、自我修正、默认 3 次重复、随机化执行顺序和 Token/耗时统计区间。

### Changed

- CI 与 Release 统一执行后端隐私回归和 72% 总覆盖率底线；圆桌页明确区分精确 tokenizer 与保守估算。
- 旧资料空间写 API 默认退役并返回 `410 FEATURE_RETIRED`；历史数据继续只读兼容，临时写入必须显式开启环境变量。
- Release 增加前端生产依赖审计、单元测试和生产构建门禁，E2E 不再依赖开发服务器。

### Security

- 诊断包默认只输出白名单字段，并由测试验证问题、日志密钥、手机令牌、模型名和主机路径不会进入归档。
- 幂等键绑定请求指纹并拒绝载荷复用或并发占用；数据库迁移备份在支持的平台限制为当前用户读写。

## [0.7.0] - 2026-07-29

### Added

- Run 事件写入 SQLite 并使用单调序号，SSE 支持 `id`、`Last-Event-ID` 和查询游标重放；多个标签页可独立订阅同一审议。
- 增加手机访问威胁模型、活动手机会话数量、最近访问时间和电脑端一键撤销。
- 增加版本、README、CHANGELOG、架构功能状态与打包版本的一致性检查，并纳入 CI。

### Changed

- 前端实时流断线后先刷新 Run 状态，再按 1/2/4/8/10 秒退避重连；断线和手机切回前台不再需要手动刷新。
- 手机 Cookie 改为随机会话 ID 与启动令牌 HMAC 签名，最长 12 小时；不再保存原始配对令牌。

### Security

- 电脑引导令牌与手机配对令牌相互独立，手机令牌不能申请电脑端权限；配对接口增加失败限速与临时锁定，并严格校验 Host、Origin、`Sec-Fetch-Site`、JSON Content-Type 和流式 2 KiB 请求体上限。
- 已配对客户端的状态修改请求执行同源校验；每个 Run 最多接受 8 个并发 SSE 连接，撤销或重启后旧手机会话立即失效。

### Fixed

- SSE 不再通过共享内存队列竞争消费事件，避免多标签页、网络抖动和浏览器休眠造成实时更新丢失。

## [0.6.0] - 2026-07-28

### Added

- 增加“设置 → 手机连接”：电脑启动 Council 后显示局域网地址和配对二维码，手机在同一 Wi-Fi 下扫码即可使用完整审议界面。
- 增加 Web App Manifest、主屏幕图标和安全上下文下的 Service Worker，可将 Council 添加到手机主屏幕。
- macOS、Windows、源码和自包含启动器统一开放局域网前端，并自动生成每次启动专属的 192-bit 配对令牌。

### Changed

- 前端 API、事件流和报告导出统一使用同源地址，由 Next.js 在电脑本机代理到只监听 loopback 的后端。

### Security

- 局域网入口默认拒绝未配对设备；扫码令牌通过 URL fragment 传给浏览器，再以 POST 换取 HttpOnly、SameSite Cookie，不进入请求 URL 或服务访问日志。
- 手机配对令牌只保存在当前用户的日志目录，停止或重启 Council 后轮换；后端与 CC Switch 始终不直接暴露到局域网。

## [0.5.1] - 2026-07-28

### Fixed

- Council 重启后不再把 CC Switch 成功请求历史中已验证的模型误判为旧版占位模型；模型下拉和已保存的五席配置会继续保留。

## [0.5.0] - 2026-07-28

### Added

- 首次打开时明确区分本地演示与真实 AI，并引导用户依次连接 Provider、配置五个席位，再开始正式审议。
- Provider 连接成功和席位保存完成后提供连续的下一步入口，减少新用户在设置页面之间寻找路径。

### Changed

- 从侧栏和新建审议主流程移除资料空间；旧项目、来源和历史 Run 快照仍保留在后端，升级不会删除已有兼容数据。
- 运行时限改为每个席位独立等待上限，不再用整桌倒计时覆盖四席讨论、用户确认和总结阶段。
- 设置页明确显示“每席最长等待 120 秒”，超时时只提示重试当前席位。

### Fixed

- 四席发言完成后等待用户确认不再触发整桌超时，耗时较长但单席均正常完成的讨论可继续保留在确认阶段。
- CC Switch 同一席位的推理降档尝试共享单席等待预算，不会因降档提前截断或重复获得完整超时时间。
- 混合 Mock/真实 Provider、未就绪 Provider 和配置读取失败不再绕过首次设置；含本地演示席时始终明确披露并要求确认。
- 保存席位后只在五席均使用已就绪真实 Provider 时显示完成入口，演示或未就绪配置仍可保存但不会误报完成。
- 首次演示请求不再携带已从界面移除的资料空间字段，旧 `/projects` 地址会安全返回新建审议。

## [0.4.0] - 2026-07-28

### Added

- 增加启动自动版本检查和“设置 → 软件更新”，已安装用户可在应用内下载、校验、替换并重启。
- 增加 macOS 独立更新助手；应用位于受保护目录时通过系统授权替换，并在失败时恢复旧应用。
- 增加 Windows 独立更新助手，停止当前服务后先备份、再原地更新完整解压目录；失败时恢复旧版，并保留桌面快捷方式。

### Security

- 更新器只读取固定 Council Lab 仓库的正式 GitHub Release，只接受精确匹配当前系统和版本的资产名称。
- 安装前强制核对同一 Release 的 `SHA256SUMS.txt`，限制下载与解压体积，并拒绝 ZIP 路径穿越和越界符号链接。
- macOS 解压恢复合法 Python Framework 符号链接与执行权限，并通过深度 codesign 验证后再进入替换阶段。
- 安装接口要求软件前端专用请求头，阻止普通网页跨站触发本机更新与重启。
- 本地后端限制为 loopback Host；强制刷新 Release 信息同样要求软件前端专用请求头，降低 DNS rebinding 与跨站消耗 GitHub API 额度的风险。

### Fixed

- Release 徽章改用 ASCII 标签，避免 GitHub Camo 对未编码中文查询参数返回 502。
- macOS 启停脚本统一使用物理路径，避免应用从带系统路径别名或符号链接的位置启动后遗漏前端进程。

## [0.3.0] - 2026-07-28

### Added

- 增加资料空间，可导入文字、公开网页、PDF、DOCX、Markdown、CSV、JSON 和 TXT，并在 Run 中冻结 SHA-256 资料快照与 `[S1]` 引用。
- 增加开放讨论、决策评审、风险审计、资料研判和事前验尸五种审议模板，以及最近项目结论上下文。
- 增加 Markdown 与自包含 HTML 报告导出。
- 增加 12 案例可重复评测框架、三种策略执行数据、匿名盲评材料和人工评分汇总。
- 增加决策结果回访，记录最终选择、预期/实际结果、复盘日期和四席观点验证状态。
- 评测增加引用支持率、未经支持主张数量、可选成本估算，以及全真实 Provider + 完整盲评门槛。
- 增加无需系统 Python/Node 的 macOS 与 Windows 自包含 Release 构建、打包后 Smoke test 和桌面快捷方式。
- Release 同时发布 `SHA256SUMS.txt`，便于校验下载包完整性。

### Changed

- 模型设置改为面向新手的凭据 → 模型 → 验证流程；“保存并测试”会先保存密钥、获取模型、选择模型，再执行最小生成测试。
- 用 `model_source` 明确区分 Provider 实时目录、CC Switch 近期成功记录、内置 Mock 与离线推荐，不再把推荐模型冒充账号实际可用模型。
- CC Switch 不可达或模型目录为空时显示可操作提示，不再仅凭 Council 自身可运行就宣称路由已连接。
- 全站更新为克制的暖纸张、墨色文字与陶土强调色界面，并保留单屏工作台和内部滚动讨论。
- 首页把资料、模板、历史上下文和运行档位整合进同一视口；讨论页增加逐席公开批注与资料来源条。
- Run 改为冻结所选资料完整正文，模型调用时再按上下文预算裁剪；导出报告可携带完整快照与结果回访。
- 模型目录空返回或连接失败时主动移除旧实时目录，避免把缓存模型继续标成实时可用。
- 长资料和历史上下文场景优先保留原问题、最新用户插话和最近发言。
- 中英文安装说明同时覆盖 macOS 与 Windows 普通用户，并明确签名、公证与源码依赖边界。

### Fixed

- macOS 停止入口会在优雅退出超时后清理 Council 自身的残留网页进程；Release 验证同时检查 PID 与服务端口，避免发布无法完整停止的安装包。

### Security

- 网页资料导入限制公开 HTTP/HTTPS 目标、DNS 解析、重定向、响应类型和大小，拒绝本机、内网、云元数据与保留地址。
- API Key 继续只保存到系统凭据库；资料删除不会静默删除历史 Run 中已冻结的审计快照。

## [0.2.2] - 2026-07-28

### Added

- 增加 Windows 10 / 11 双击安装、启动和停止入口，并自动创建桌面快捷方式。
- GitHub Release 同时生成 macOS 与 Windows 下载包，CI 增加 `windows-latest` 脚本校验。
- 后端依赖锁文件改为跨平台解析，Windows 自动跳过不兼容的 `uvloop` 并安装系统凭据库支持。

## [0.2.1] - 2026-07-28

### Fixed

- 将默认 Provider 累计 Token 边界从 12k 调整为 40k，覆盖 CC Switch Codex 路径每次约 4k-5k 的基础 instructions 开销。
- 圆桌页分开显示单次讨论上下文与 Provider 全程累计 usage，避免把 `630 / 4000` 与全局限额混为一谈。
- 达到调用或 Token 边界的 Run 可提高限额并从未完成席位继续，已完成席位不会重复请求。

## [0.2.0] - 2026-07-27

### Added

- 四个讨论席与总结席的独立 Provider/model 配置、SQLite 持久化和 Run 快照。
- 第四席之后的用户确认点，支持多次最终补充再生成答案。
- 启动扫描与 checkpoint 恢复，以及缺少 checkpoint/凭据时的可恢复失败状态。
- 模型调用数、累计 Token 和完整运行时间的后端强制边界。
- Provider 原生 reasoning 能力标识和逐席实际 Provider/model 记录。

### Changed

- 用确定性上下文裁剪替代未实现的语义摘要描述。
- 最终结论改为定性“未核验”，仅从明确表态中提取分歧。
- 设置与评测页面只展示真实持久化配置和真实可用数据。
- 四席结束后默认不再自动总结。

### Security

- 自定义 Provider 在连接前解析全部 DNS 结果并拒绝 metadata、link-local、unspecified、multicast 和 reserved 目标。
- 所有临时与运行期 HTTP 客户端在成功、失败和取消路径关闭。

### Removed

- 未实现的联网核验、代码沙箱、附件、美元预算和静态设置控件。
- 将所有工作流档位宣称为上游原生 Low / High / Ultra 的误导文案。

## [0.1.0] - 2026-07-21

### Added

- LangGraph 四席顺序讨论、用户插话、持久 checkpoint 和自动总结。
- CC Switch、DeepSeek、智谱 GLM、Kimi、硅基流动、OpenAI 与自定义兼容 Provider。
- 跨平台用户数据目录、系统凭据库、模型自动识别和 macOS 启动器。
- 开源许可证、贡献、安全、行为规范、持续集成与 Release 工作流。
