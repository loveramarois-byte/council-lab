# Changelog

所有重要变化记录在此文件。版本遵循 Semantic Versioning。

## [Unreleased]

## [0.6.0] - 2026-07-28

### Added

- 增加“设置 → 手机连接”：电脑启动 Council 后显示局域网地址和配对二维码，手机在同一 Wi-Fi 下扫码即可使用完整审议界面。
- 增加 Web App Manifest、主屏幕图标和安全上下文下的 Service Worker，可将 Council 添加到手机主屏幕。
- macOS、Windows、源码和自包含启动器统一开放局域网前端，并自动生成每次启动专属的 192-bit 配对令牌。

### Changed

- 前端 API、事件流和报告导出统一使用同源地址，由 Next.js 在电脑本机代理到只监听 loopback 的后端。

### Security

- 局域网入口默认拒绝未配对设备；扫码令牌通过 URL fragment 传给浏览器，再以 POST 换取 HttpOnly、SameSite Cookie，不进入请求 URL 或服务访问日志。
- 手机配对令牌只保存在当前用户的日志目录，停止或重启 Council 后轮换；后端与 CC Switch 始终不直接暴露到局域网。

## [0.5.1] - 2026-07-28

### Fixed

- Council 重启后不再把 CC Switch 成功请求历史中已验证的模型误判为旧版占位模型；模型下拉和已保存的五席配置会继续保留。

## [0.5.0] - 2026-07-28

### Added

- 首次打开时明确区分本地演示与真实 AI，并引导用户依次连接 Provider、配置五个席位，再开始正式审议。
- Provider 连接成功和席位保存完成后提供连续的下一步入口，减少新用户在设置页面之间寻找路径。

### Changed

- 从侧栏和新建审议主流程移除资料空间；旧项目、来源和历史 Run 快照仍保留在后端，升级不会删除已有兼容数据。
- 运行时限改为每个席位独立等待上限，不再用整桌倒计时覆盖四席讨论、用户确认和总结阶段。
- 设置页明确显示“每席最长等待 120 秒”，超时时只提示重试当前席位。

### Fixed

- 四席发言完成后等待用户确认不再触发整桌超时，耗时较长但单席均正常完成的讨论可继续保留在确认阶段。
- CC Switch 同一席位的推理降档尝试共享单席等待预算，不会因降档提前截断或重复获得完整超时时间。
- 混合 Mock/真实 Provider、未就绪 Provider 和配置读取失败不再绕过首次设置；含本地演示席时始终明确披露并要求确认。
- 保存席位后只在五席均使用已就绪真实 Provider 时显示完成入口，演示或未就绪配置仍可保存但不会误报完成。
- 首次演示请求不再携带已从界面移除的资料空间字段，旧 `/projects` 地址会安全返回新建审议。

## [0.4.0] - 2026-07-28

### Added

- 增加启动自动版本检查和“设置 → 软件更新”，已安装用户可在应用内下载、校验、替换并重启。
- 增加 macOS 独立更新助手；应用位于受保护目录时通过系统授权替换，并在失败时恢复旧应用。
- 增加 Windows 独立更新助手，停止当前服务后先备份、再原地更新完整解压目录；失败时恢复旧版，并保留桌面快捷方式。

### Security

- 更新器只读取固定 Council Lab 仓库的正式 GitHub Release，只接受精确匹配当前系统和版本的资产名称。
- 安装前强制核对同一 Release 的 `SHA256SUMS.txt`，限制下载与解压体积，并拒绝 ZIP 路径穿越和越界符号链接。
- macOS 解压恢复合法 Python Framework 符号链接与执行权限，并通过深度 codesign 验证后再进入替换阶段。
- 安装接口要求软件前端专用请求头，阻止普通网页跨站触发本机更新与重启。
- 本地后端限制为 loopback Host；强制刷新 Release 信息同样要求软件前端专用请求头，降低 DNS rebinding 与跨站消耗 GitHub API 额度的风险。

### Fixed

- Release 徽章改用 ASCII 标签，避免 GitHub Camo 对未编码中文查询参数返回 502。
- macOS 启停脚本统一使用物理路径，避免应用从带系统路径别名或符号链接的位置启动后遗漏前端进程。

## [0.3.0] - 2026-07-28

### Added

- 增加资料空间，可导入文字、公开网页、PDF、DOCX、Markdown、CSV、JSON 和 TXT，并在 Run 中冻结 SHA-256 资料快照与 `[S1]` 引用。
- 增加开放讨论、决策评审、风险审计、资料研判和事前验尸五种审议模板，以及最近项目结论上下文。
- 增加 Markdown 与自包含 HTML 报告导出。
- 增加 12 案例可重复评测框架、三种策略执行数据、匿名盲评材料和人工评分汇总。
- 增加决策结果回访，记录最终选择、预期/实际结果、复盘日期和四席观点验证状态。
- 评测增加引用支持率、未经支持主张数量、可选成本估算，以及全真实 Provider + 完整盲评门槛。
- 增加无需系统 Python/Node 的 macOS 与 Windows 自包含 Release 构建、打包后 Smoke test 和桌面快捷方式。
- Release 同时发布 `SHA256SUMS.txt`，便于校验下载包完整性。

### Changed

- 模型设置改为面向新手的凭据 → 模型 → 验证流程；“保存并测试”会先保存密钥、获取模型、选择模型，再执行最小生成测试。
- 用 `model_source` 明确区分 Provider 实时目录、CC Switch 近期成功记录、内置 Mock 与离线推荐，不再把推荐模型冒充账号实际可用模型。
- CC Switch 不可达或模型目录为空时显示可操作提示，不再仅凭 Council 自身可运行就宣称路由已连接。
- 全站更新为克制的暖纸张、墨色文字与陶土强调色界面，并保留单屏工作台和内部滚动讨论。
- 首页把资料、模板、历史上下文和运行档位整合进同一视口；讨论页增加逐席公开批注与资料来源条。
- Run 改为冻结所选资料完整正文，模型调用时再按上下文预算裁剪；导出报告可携带完整快照与结果回访。
- 模型目录空返回或连接失败时主动移除旧实时目录，避免把缓存模型继续标成实时可用。
- 长资料和历史上下文场景优先保留原问题、最新用户插话和最近发言。
- 中英文安装说明同时覆盖 macOS 与 Windows 普通用户，并明确签名、公证与源码依赖边界。

### Fixed

- macOS 停止入口会在优雅退出超时后清理 Council 自身的残留网页进程；Release 验证同时检查 PID 与服务端口，避免发布无法完整停止的安装包。

### Security

- 网页资料导入限制公开 HTTP/HTTPS 目标、DNS 解析、重定向、响应类型和大小，拒绝本机、内网、云元数据与保留地址。
- API Key 继续只保存到系统凭据库；资料删除不会静默删除历史 Run 中已冻结的审计快照。

## [0.2.2] - 2026-07-28

### Added

- 增加 Windows 10 / 11 双击安装、启动和停止入口，并自动创建桌面快捷方式。
- GitHub Release 同时生成 macOS 与 Windows 下载包，CI 增加 `windows-latest` 脚本校验。
- 后端依赖锁文件改为跨平台解析，Windows 自动跳过不兼容的 `uvloop` 并安装系统凭据库支持。

## [0.2.1] - 2026-07-28

### Fixed

- 将默认 Provider 累计 Token 边界从 12k 调整为 40k，覆盖 CC Switch Codex 路径每次约 4k-5k 的基础 instructions 开销。
- 圆桌页分开显示单次讨论上下文与 Provider 全程累计 usage，避免把 `630 / 4000` 与全局限额混为一谈。
- 达到调用或 Token 边界的 Run 可提高限额并从未完成席位继续，已完成席位不会重复请求。

## [0.2.0] - 2026-07-27

### Added

- 四个讨论席与总结席的独立 Provider/model 配置、SQLite 持久化和 Run 快照。
- 第四席之后的用户确认点，支持多次最终补充再生成答案。
- 启动扫描与 checkpoint 恢复，以及缺少 checkpoint/凭据时的可恢复失败状态。
- 模型调用数、累计 Token 和完整运行时间的后端强制边界。
- Provider 原生 reasoning 能力标识和逐席实际 Provider/model 记录。

### Changed

- 用确定性上下文裁剪替代未实现的语义摘要描述。
- 最终结论改为定性“未核验”，仅从明确表态中提取分歧。
- 设置与评测页面只展示真实持久化配置和真实可用数据。
- 四席结束后默认不再自动总结。

### Security

- 自定义 Provider 在连接前解析全部 DNS 结果并拒绝 metadata、link-local、unspecified、multicast 和 reserved 目标。
- 所有临时与运行期 HTTP 客户端在成功、失败和取消路径关闭。

### Removed

- 未实现的联网核验、代码沙箱、附件、美元预算和静态设置控件。
- 将所有工作流档位宣称为上游原生 Low / High / Ultra 的误导文案。

## [0.1.0] - 2026-07-21

### Added

- LangGraph 四席顺序讨论、用户插话、持久 checkpoint 和自动总结。
- CC Switch、DeepSeek、智谱 GLM、Kimi、硅基流动、OpenAI 与自定义兼容 Provider。
- 跨平台用户数据目录、系统凭据库、模型自动识别和 macOS 启动器。
- 开源许可证、贡献、安全、行为规范、持续集成与 Release 工作流。
