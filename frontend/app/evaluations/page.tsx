"use client";

import Link from "next/link";
import { ArrowUpRight, BarChart3, CheckCircle2, FlaskConical, Minus } from "lucide-react";

export default function EvaluationsPage() { return <div className="page-wrap"><header className="topbar"><div><span className="top-kicker">工作台 / 评测</span><span className="top-title">评测台</span></div><span className="top-meta"><span className="status-dot" />仅使用本地 Mock 数据</span></header><div className="list-intro"><p className="eyebrow terracotta">EVALUATION / 03</p><h1>先测系统，再相信答案。</h1><p>用固定问题集比较模式、验证覆盖与成本。没有真实 API 时，不伪造模型表现。</p></div><div className="eval-grid"><div className="eval-hero"><FlaskConical size={20} /><span className="eyebrow">当前基线</span><strong>尚未运行评测</strong><p>添加问题集后，系统会记录每种运行模式的延迟、Token 与证据覆盖。</p><button className="send-button disabled"><BarChart3 size={16} />新建评测集</button></div><div className="eval-table"><div className="eval-row eval-header"><span>指标</span><span>快速</span><span>标准</span><span>严谨</span></div>{[["事实准确率", "—", "—", "—"], ["验证覆盖率", "—", "—", "—"], ["平均耗时", "—", "—", "—"], ["平均模型调用", "1", "3", "4"]].map((row) => <div className="eval-row" key={row[0]}>{row.map((cell, index) => index === 0 ? <span key={cell}>{cell}</span> : <span key={`${cell}-${index}`} className="muted-cell">{cell}</span>)}</div>)}</div></div><div className="eval-note"><CheckCircle2 size={16} /><span>当前演示默认使用 Mock Provider。评测结果只在有真实运行记录后出现。</span><Link href="/" className="text-action">去新建审议 <ArrowUpRight size={14} /></Link></div></div>; }
