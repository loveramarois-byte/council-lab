"use client";

import Link from "next/link";
import { ArrowUpRight, BarChart3, CheckCircle2, FlaskConical, Minus } from "lucide-react";

export default function EvaluationsPage() { return <div className="page-wrap"><header className="topbar"><div><span className="top-kicker">工作台 / 评测</span><span className="top-title">评测台</span></div><span className="top-meta"><span className="status-dot" />暂无真实评测数据</span></header><div className="list-intro"><p className="eyebrow terracotta">可信度基线</p><h1>先测系统，再相信答案。</h1><p>用固定问题集比较单模型、同模型四角色和跨模型四席。没有真实数据时不展示推测结果。</p></div><div className="eval-grid"><div className="eval-hero"><FlaskConical size={20} /><span className="eyebrow">当前基线</span><strong>尚未运行评测</strong><p>未来真实评测会记录准确率、遗漏、延迟、Token、成本与 Provider 失败率。</p><span className="send-button disabled"><BarChart3 size={16} />评测集尚未接入</span></div><div className="eval-table"><div className="eval-row eval-header"><span>指标</span><span>快速</span><span>标准</span><span>严谨</span></div>{[["事实准确率", "—", "—", "—"], ["验证覆盖率", "—", "—", "—"], ["平均耗时", "—", "—", "—"], ["平均模型调用", "—", "—", "—"]].map((row) => <div className="eval-row" key={row[0]}>{row.map((cell, index) => index === 0 ? <span key={cell}>{cell}</span> : <span key={`${cell}-${index}`} className="muted-cell">{cell}</span>)}</div>)}</div></div><div className="eval-note"><CheckCircle2 size={16} /><span>Mock 只验证工作流行为，不代表真实模型效果。</span><Link href="/" className="text-action">去新建审议 <ArrowUpRight size={14} /></Link></div></div>; }
