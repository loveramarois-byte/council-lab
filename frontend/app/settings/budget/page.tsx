import Link from "next/link";
import { ArrowLeft, Gauge, ShieldCheck } from "lucide-react";
export default function BudgetSettingsPage() { return <div className="page-wrap simple-settings"><header className="topbar"><div><Link href="/settings/providers" className="back-link"><ArrowLeft size={15} />设置</Link><span className="top-title">预算与限制</span></div></header><div className="settings-heading"><p className="eyebrow terracotta">BUDGET / 06</p><h1>在开始之前，把边界说清楚。</h1><p>每次运行都会受到代理数、模型调用次数、Token 和时间上限约束。</p></div><div className="limit-grid"><div className="limit-card"><Gauge size={18} /><span>标准模式</span><strong>3 个独立代理</strong><small>最多 16 次模型调用 · 12k Token</small></div><div className="limit-card"><ShieldCheck size={18} /><span>成本提示</span><strong>Mock Provider</strong><small>当前演示不会产生外部 API 费用</small></div></div><div className="settings-note">预算上限和重试次数会在后端执行；不是模型的一句自我承诺。</div></div>; }
