import Link from "next/link";
import { ArrowLeft, Gauge, ShieldCheck } from "lucide-react";
export default function BudgetSettingsPage() { return <div className="page-wrap simple-settings"><header className="topbar"><div><Link href="/settings/providers" className="back-link"><ArrowLeft size={15} />设置</Link><span className="top-title">运行限制</span></div></header><div className="settings-heading"><p className="eyebrow terracotta">LIMITS / 06</p><h1>限制由后端真正执行。</h1><p>产品固定四个讨论席和一个总结席。每次调用前都会检查模型调用数、累计 Token 与完整运行时间。</p></div><div className="limit-grid"><div className="limit-card"><Gauge size={18} /><span>默认运行边界</span><strong>4 席 + 1 总结席</strong><small>正常 5 次，含失败重试最多 8 次 · 12k Token · 120 秒</small></div><div className="limit-card"><ShieldCheck size={18} /><span>费用边界</span><strong>不伪造美元预算</strong><small>缺少可靠价格数据时只显示真实 Token 与调用数</small></div></div><div className="settings-note">达到限制后，运行会明确停止并保留已经完成的发言；这些默认值目前为只读。</div></div>; }
