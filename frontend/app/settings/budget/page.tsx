import Link from "next/link";
import { ArrowLeft, Gauge, ShieldCheck } from "lucide-react";
export default function BudgetSettingsPage() { return <div className="page-wrap simple-settings"><header className="topbar"><div><Link href="/settings/providers" className="back-link"><ArrowLeft size={15} />设置</Link><span className="top-title">运行限制</span></div></header><div className="settings-heading"><p className="eyebrow terracotta">LIMITS / 06</p><h1>限制由后端真正执行。</h1><p>产品固定四个讨论席和一个总结席。上下文窗口控制单次发送内容，累计额度则按 Provider 返回的真实用量执行。</p></div><div className="limit-grid"><div className="limit-card"><Gauge size={18} /><span>默认运行边界</span><strong>4 席 + 1 总结席</strong><small>正常 5 次，含失败重试最多 8 次 · 40k Provider Token · 每席最长等待 120 秒</small></div><div className="limit-card"><ShieldCheck size={18} /><span>CC Switch 计数</span><strong>包含上游基础指令</strong><small>Codex 路径每次可能额外返回约 4k-5k 输入 Token，以真实 usage 为准</small></div></div><div className="settings-note">没有整桌倒计时。只有单个席位调用超时，或达到调用次数与 Token 额度时才会停止；已完成发言会保留。</div></div>; }
